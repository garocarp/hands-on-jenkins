pipeline {
    agent any    
    stages {
        stage('Test') {
            steps {
                withEnv(['PATH+EXTRA=/usr/sbin:/usr/bin:/sbin:/bin'])  
                sh 'env' 
                sh 'docker --version'
            }
        }
    }
}
